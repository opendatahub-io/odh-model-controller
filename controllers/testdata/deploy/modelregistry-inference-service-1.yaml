apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: dummy-inference-service
  namespace: default
  labels:
    "model-registry-is-id": "4"
    "model-registry-sm-id": "1"
spec:
  predictor:
    model:
      modelFormat:
        name: onnx
        version: 1
      runtime: ovms-1.x
      storage:
        key: aws-connection-models
        path: /path/to/old/model
