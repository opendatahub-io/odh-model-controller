---
# ==============================================================================
# Hadolint Configuration for Dockerfile Security Analysis
# ==============================================================================
#
# PURPOSE:
# Enforces Docker best practices and CIS Benchmark recommendations for
# Dockerfiles. Catches common security issues like unpinned base images,
# secrets in ENV vars, and running containers as root.
#
# USED BY:
# - GitHub Actions: .github/workflows/security-full-scan.yml
# - Local usage: hadolint --config .github/config/hadolint.yaml Dockerfile
#
# PATH EXCLUSIONS: Only test fixtures, examples, docs, and vendor
# Removed: contrib, scripts, bin, .github/workflows (no Dockerfiles exist there)
# ==============================================================================

# Ignore Dockerfiles in test/example/vendor directories to prevent false positives
# Use glob patterns to match nested directories
ignored:
  - "**/examples/**"
  - "**/testdata/**"
  - "**/test/**"
  - "**/tests/**"
  - "**/docs/**"
  - "**/vendor/**"

# Optional: Disable specific rules if they create too many false positives
# ignored-rules:
#   - DL3008  # Pin versions in apt-get install
#   - DL3018  # Pin versions in apk add
