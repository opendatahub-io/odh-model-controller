apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: dummy-inference-service
  namespace: default
  labels:
    mr-registered-model-id: 2
    mr-model-version-id: 3
spec:
  predictor:
    model:
      modelFormat:
        name: onnx
      runtime: ovms-1.x
      storage:
        key: testkey
        path: /testpath/test